multi:
type: ipv4net,ipv6net
help: Subnet of client(s) allowed to use webproxy
val_help: ipv4net; IP address and prefix length
val_help: ipv6net; IPv6 address and prefix length

syntax:expression: exec "/usr/bin/ipaddrcheck --verbose --is-any-net $VAR(@)"
